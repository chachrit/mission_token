# Uploads (user content)
uploads/submissions/*
!uploads/submissions/.gitkeep

# PHP error logs
*.log

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp

# Database credentials — never commit, manage per-environment
config/database.php

# One-time setup script — never commit
setup_git.php

# deploy.php is committed but keep this note:
# The secret in deploy.php is not sensitive enough to exclude
# as it only triggers git pull (no shell injection possible)
